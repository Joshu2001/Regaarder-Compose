# Regaarder Compose

## Setup
1. Install dependencies: npm install
2. Run dev: npm run dev

## Vercel Deployment
This project is configured for Vercel using 
pm run build as the build command and dist as the output directory.
