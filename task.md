# Slash Commands (/) and AI Preview Blocks Implementation

- `[ ]` Define slash menu popover state and UI in `src/App.jsx`
- `[ ]` Implement editor event listener for keydown `/` and popover rendering coordinates
- `[ ]` Implement popover list navigation (Up/Down arrow keys, Enter to select, Escape to close)
- `[ ]` Implement in-line prompt input box rendering (`inline-ai-prompt-box`) at text cursor
- `[ ]` Implement AI generation router (`handleAIBlockSubmit`) for Table, Bullet points, SVG Graphs, Images, Translate, and Proofread
- `[ ]` Implement inline SVG chart builder (`renderSvgChart`) to draw bar, line, and pie charts based on Gemini-returned datasets
- `[ ]` Implement preview container wrapping and the Review/Action Banner (`Accept`, `Retry`, `Delete`, `Export`)
- `[ ]` Implement refinement / retry execution using the inline banner text input
- `[ ]` Implement export HTML sanitization to strip preview containers and action buttons
- `[ ]` Test and build the application
- `[ ]` Commit and push to GitHub
